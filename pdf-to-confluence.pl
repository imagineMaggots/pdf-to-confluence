#!/usr/bin/perl
use strict;
use warnings;
# use REST::Client;

my ($url, $token) = @ARGV
print "URL = '$url'";
print "Token = '$token'";

# my $rest_client = REST::Client->new();

print "Hello World";
